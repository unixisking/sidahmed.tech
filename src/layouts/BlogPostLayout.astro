---
import Layout from './Layout.astro'

const { title, minutesRead, excerpt } = Astro.props.frontmatter || Astro.props
---

<Layout title={title} description={excerpt}>
  <main class="prose dark:prose-invert prose-neutral">
    {minutesRead}
    <slot />
  </main>
</Layout>
